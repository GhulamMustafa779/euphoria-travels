import SectionTitle from "@/components/ui/SectionTitle";
import SubTitle from "@/components/ui/SubTitle";
import React from "react";

const Blogs = () => {
	return (
		<div>
			<SectionTitle bgText='Blogs' title='Travel Blogs' />
			<SubTitle text="Read our latest travel blogs and stay updated with the latest travel trends and tips." />
		</div>
	);
};

export default Blogs;
